@using Blogifier.Core.Common
@{
    Layout = $"~/{ApplicationSettings.BlogAdminFolder}/_Layout/_Layout.cshtml";
}
<div class="bf-header d-flex d-lg-none">Settings / <span></span> <i class="fa fa-chevron-down"></i></div>
<div class="bf-body">
    <div class="bf-sidebar d-lg-block">
        <div class="bf-settings-sidebar">
            @if (Model.Profile.IsAdmin)
            {
                <ul class="bf-settings-sidebar-nav">
                    <li><a href="~/admin/settings/general">General</a></li>
                    <li><a href="~/admin/settings/posts">Posts</a></li>
                </ul>
            }
            <ul class="bf-settings-sidebar-nav">
                <li><a href="~/admin/settings/profile">Profile</a></li>
                <li><a href="~/account/changepassword">Password</a></li>
                @if (Model.Profile.IsAdmin)
                {
                    <li><a href="~/admin/settings/users">Users</a></li>
                }
            </ul>
            @if (Model.Profile.IsAdmin)
            {
                <ul class="bf-settings-sidebar-nav">
                    <li><a href="~/admin/settings/advanced">Advanced</a></li>
                    <li><a href="~/admin/settings/about">About</a></li>
                </ul>
            }
        </div>
    </div>
    <div class="bf-content">
        @RenderBody()
    </div>
</div>
@section Scripts{
    @RenderSection("Scripts", false)
}